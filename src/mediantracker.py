class MedianTracker(object):
    def receive(self, updated_counts):
        """Receives a list of degree updates (one for each added/deleted/updated node) and returns the median

        input
        -------
        list of degree updates [{<old_degree>: <new_degree>}, ...]

        return
        -------
        current median of the node degrees
        """
        return 0
